<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>
        <%= title %>
    </title>
    <!-- CSS Section-->
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/@fortawesome/fontawesome-free/css/all.min.css" />
    <link rel="stylesheet" href="/Content/app.css" />

    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-150534686-1"></script>
    <script>
        window.dataLayer = window.dataLayer || [];

        function gtag() {
            dataLayer.push(arguments);
        }
        gtag('js', new Date());
        gtag('config', 'UA-150534686-1');
    </script>

</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/"><span class="letter-spacing-5"><span
                    class="font-weight-bold">CEN</span>EATS</span></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <% if(title == "Home") { %>
                    <li class="nav-item active">
                        <% } else { %>
                    </li>
                    <li class="nav-item">
                        <% } %>
                            <a class="nav-link" href="/"><i class="fas fa-lg fa-home"></i> Home
                        <span class="sr-only">(current)</span></a>
                    </li>

                    <% if(title == "Order List") { %>
                        <li class="nav-item active">
                            <% } else { %>
                        </li>
                        <li class="nav-item">
                            <% } %>
                                <a class="nav-link" href="/order-list" id="orderListBtn"><i class="far fa-paper-plane"></i> Orders</a>
                        </li>

                        <% if(title == "Services") { %>
                            <li class="nav-item active">
                                <% } else { %>
                            </li>

                            <li class="nav-item">
                                <% } %>

                                    <a class="nav-link" href="/review-list" id="reviewListBtn"><i class="far fa-comment-dots"></i> Reviews</a>
                            </li>

                            <% if(title == "Statistics") { %>
                                <li class="nav-item active">
                                    <% } else { %>
                                </li>

                                <li class="nav-item">
                                    <% } %>

                                        <a class="nav-link" href="/stats"><i class="far fa-chart-bar"></i> Stats</a>
                                </li>

                                <% if(typeof displayName == 'string' && displayName) { %>
                                    <li class="navbar-text"><i class="fas fa-lg fa-user"></i> Welcome,
                                        <%= displayName %>
                                    </li>
                                    <li class="nav-item">
                                        <a href="/logout" class="nav-link">
                                            <i class="fas  fa-sign-out-alt"></i> Logout</a>
                                    </li>
                                    <% } else { %>
                                        <li class="nav-item">
                                            <a href="/login" class="nav-link" id="LoginBtn">
                                                <i class="fas  fa-sign-in-alt"></i> Login</a>
                                        </li>
                                        <% } %>
            </ul>
        </div>
    </nav>