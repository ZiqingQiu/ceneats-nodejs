<% include ../partials/header.ejs %>

    <div class="container">
        <div class="row">
            <div class="col-md-offset-3 col-md-6">
                <h1><%= title %></h1>
                <a class="btn btn-primary" href="/contact-list/add" role="button">
                <i class="fas fa-plus-circle"></i> Add a account</a>
                <br>
                <table class="table table-bordered table-striped table-hover">
                    <!-- Header Row-->
                    <tr>
                        <th class="text-center">Centennial Id</th>
                        <th class="text-center">Display Name</th>
                        <th class="text-center">Account Type</th>
                        <th class="text-center"></th>
                        <th class="text-center"></th>
                    </tr>

                    <!-- Repeatable Template Row -->
                    <% for(let count = 0; count < accountList.length; count++) { %>
                        <tr>
                            <td class="text-center"><%= accountList[count].username %></td>
                            <td class="text-center"><%= accountList[count].displayName %></td>
                            <td class="text-center"><%= accountList[count].accountType %></td>

                            <td class="text-center">
                            <a href="/contact-list/edit/<%= accountList[count]._id %>"
                                class="btn btn-primary"><i class="fas fa-pencil-alt"></i> Edit</a>
                            </td>

                            <td class="text-center">
                                    <a href="/contact-list/delete/<%= accountList[count]._id %>"
                                        class="btn btn-danger"><i class="fas fa-trash-alt"></i> Delete</a>
                                    </td>
                        </tr>
                    <% } %>
                </table>

            </div>
        </div>
    </div>

<% include ../partials/footer.ejs %>